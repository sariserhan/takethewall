import { DocumentPage, documentMetadata } from "@/components/document-page";
export const metadata = documentMetadata("support");
export default function Page() {
  return <DocumentPage info="support" />;
}
