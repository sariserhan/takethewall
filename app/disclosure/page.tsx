import { DocumentPage, documentMetadata } from "@/components/document-page";
export const metadata = documentMetadata("disclosure");
export default function Page() {
  return <DocumentPage info="disclosure" />;
}
